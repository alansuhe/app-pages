---
import BaseLayout from '@/layouts/BaseLayout.astro';
import AppCard from '@/components/AppCard.astro';
import { getCollection } from 'astro:content';
import { getTranslations } from '@/i18n/utils';

const lang = 'en';
const translations = await getTranslations(lang);

const apps = await getCollection('apps', ({ id }) => id.startsWith(`${lang}/`));
---

<BaseLayout lang={lang} title={translations?.nav.apps ?? 'Apps'}>
  <h1>{translations?.apps.allApps ?? 'All Apps'}</h1>
  <p class="page-description">
    Explore all available applications in the Apps Hub.
  </p>

  <section class="apps-grid">
    {apps.map((app) => (
      <AppCard app={app} lang={lang} />
    ))}
  </section>
</BaseLayout>

<style>
  .page-description {
    color: var(--color-text-muted);
    margin-bottom: var(--spacing-xl);
  }

  .apps-grid {
    display: grid;
    gap: var(--spacing-lg);
  }

  @media (min-width: 768px) {
    .apps-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>